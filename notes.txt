старый план:
    0.1 вывести алерты
1. понять как подгружать в профиле настройки, комментарии и фотографии -- аякс?
4. сделать вывод порционный с несколькими страницами (по сколько нужно?)
5. перечитать сабджект
6. сделать изменение данных в профиле
8. сделать кнопки подсвечивающимися
9. сделать смену фоток на главной странице
10. разные стартовые страницы для залогиненого(галлерея) и не залогиненого(логин)
11. развернуть базу данных
12. разобраться со шрифтами
13. понять как обрабатывать кнопки и понять рациональный у меня подход или нет
14. создать пользователя который все будет мочь
______________________________________________________________________________
1. сверстать : кабинет пользователя, открытая фотка, добавление фотки
htmlentities - функция для защиты

по профилю
1. понять как подгружать в профиле разные страницы: настройки, комментарии и фотографии -- аякс?

по красоте:
1. сделать кнопки подсвечивающимися
2. сделать смену фоток
3. сделать всплывающее окно на форму регистра и логина
4. вывести реакции на:
    - регистарицю
    - логин
    - подтверждение аккаунта
5. разные стартовые страницы для залогиненого(галлерея) и не залогиненого(логин)
6. сверстать загрузку фотки, открытую фотку
7. добавить лайки
8. добавить комментарии


export PATH=${PATH}:/Users/ffood/.brew/opt/mysql@5.7/bin
/Users/ffood/.brew/opt/mysql@5.7/bin/mysql.server start

потом сделать порционный вывод, лайки и начать возиться с версткой
после комментарии

нужно ли и где
$pdo = null;
?

сегодня:
сделать алерты у форм
подтверждение аккаунта и отправка писем 
сделать сложность паролю
сделать отдельный файл/функцию connect_to_database



for ($i = 1; $i <= $total_pages; $i++) {
echo "<a id='paginations' href='greetingcards.php?page=" . $i . "&displayAmount=".$selected."'>" . $i . "</a> ";
}


                $count_photos = count($photos_array);
	            $photos_per_page = 6;
	            $pages = ceil($count_photos / $photos_per_page);


на этой неделе
закончить лайки, отправку писем и аллерты, сделать минимальную верстку
на следующей - комментарии, камеру и верстку к ним
на следующей - стикеры и адаптивную верстку
на следующей - аякс, бонусы, сдача